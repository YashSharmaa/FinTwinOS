apiVersion: v1
kind: Service
metadata:
  name: {{ include "fintwinos.fullname" . }}
  labels:
    {{- include "fintwinos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "fintwinos.selectorLabels" . | nindent 4 }}
