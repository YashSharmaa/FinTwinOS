apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fintwinos.fullname" . }}-env
  labels:
    {{- include "fintwinos.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
